package com.uniquindio.trabajogrado.SIODUQ.util;

public class Uitilidades {
    
    public static String rutaFormulario(String codigo){
        
        /**if (codigo.equals(Constantes.FBARTICULO)) {
            
        }
        if (codigo.equals(Constantes.FBDIRECCIONT)) {
            
        }*/
        if (codigo.equals(Constantes.FBESTUDIOPOS)) {
            return "gestionarFormularioEstudioPosdoctoral";
        }
        /**if (codigo.equals(Constantes.FBOTRAREVIS)) {
            
        }
        if (codigo.equals(Constantes.FBPONENCIA)) {
            
        }
        if (codigo.equals(Constantes.FPAARTICULO)) {
            
        }
        if (codigo.equals(Constantes.FPAAUDIOV)) {
            
        }
        if (codigo.equals(Constantes.FPALIBROE)) {
            
        }
        if (codigo.equals(Constantes.FPALIBROI)) {
            
        }
        if (codigo.equals(Constantes.FPAOBRAARTIS)) {
            
        }
        if (codigo.equals(Constantes.FPALIBROT)) {
            
        }
        if (codigo.equals(Constantes.FPAPATENTE)) {
            
        }
        if (codigo.equals(Constantes.FPAPRODTEC)) {
            
        }
        if (codigo.equals(Constantes.FPASOFT)) {
            
        }
        if (codigo.equals(Constantes.FPAVALLIBROT)) {
            
        }*/
        
        return "";
    }
}
